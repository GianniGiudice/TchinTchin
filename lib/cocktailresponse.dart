import 'package:flutter/widgets.dart';

class cocktailResponse extends StatelessWidget {
  const cocktailResponse({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}